# Hackathon 2026

Project scaffold for the Hackathon 2026 incident response application.

## Structure

- `frontend/` contains the static web pages, scripts, styles, and assets.
- `backend/` contains the Express server, API routes, controllers, services, and local data.

## Run the backend

```bash
npm start
```

The server runs on port `5000` by default.
